For those feedback, write it into REPORT_followup.md, using the same rules as writing in REPORT.md
1. Add a pairwise plot remove the characters that are undertrained (<1000 frequency) and look for trends
2. Add some example plots showing d(t) plot interpolating from & to well trained characters, no metrics need to be reported just visually check if they are asymmetrical
4. Character level GPT, check if they have context in the plots of REPORT.md. State that in the plot and also state how many samples are used in each cell of the plot
6. Make another pairwise plot from a letter to other characters, a letter is well trained, and plot after <1000 frequency, check if they are grouped by some semantic groups