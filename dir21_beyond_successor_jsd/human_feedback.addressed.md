There is a major control-matching problem: the MLP and random controls are matched only on the mean output JSD across the 12 tokens, while the conclusion depends on the ordering of those individual tokens. Their per-token perturbation magnitudes may therefore be substantially mismatched. Please match or normalize output JSD separately for each token before concluding that the block-0 MLP specifically carries the transition-width ordering.
